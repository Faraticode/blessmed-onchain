import React from 'react'

function Message() {
    return (
    <div className='w-[50%] mb-[50px]'>         
        <p className='text-[12px] font-mono font-500 flex items-center justify-center text-center' style={{ color: 'rgba(255, 240, 230, 0.7)' }}>Every check-in here is a real, signed transaction on the Stacks blockchain — verifiable by anyone.</p>
    </div>

    );
  }

export default Message